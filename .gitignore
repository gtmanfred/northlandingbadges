bin/
coverage.out
coverage.txt
*.db
*.db-wal
*.db-shm

# Never commit real signing material. The only certificates in this repo are the
# throwaway test keys in internal/testkeys/.
apple-pass-cert.pem
apple-pass-key.pem
wwdr.pem
google-service-account*.json
.env
.env.*

# Local scratch: never commit. Flagged as holding a credential.
this.sh

# Apple Wallet signing material and locally minted passes. Rooted so the
# throwaway certificates under internal/testkeys stay tracked.
/*.cer
/*.csr
/*.pem
/*.p12
/*.pkpass
